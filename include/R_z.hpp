#ifndef _R_Z_
#define _R_Z_

#include "..\include\matrix.hpp"
#include <cmath>

/**
 * @details Genera una matriz de rotación alrededor del eje Z.
 * @param angle Ángulo de rotación (en radianes).
 * @return Matriz de rotación 3x3.
 */
Matrix R_z(double angle);

#endif