#ifndef _R_X_
#define _R_X_

#include "..\include\matrix.hpp"
#include <cmath>

/**
 * @details Genera una matriz de rotación alrededor del eje X.
 * @param angle Ángulo de rotación (en radianes).
 * @return Matriz de rotación 3x3.
 */
Matrix R_x(double angle);

#endif