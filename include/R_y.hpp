#ifndef _R_Y_
#define _R_Y_

#include "..\include\matrix.hpp"
#include <cmath>

/**
 * @details Genera una matriz de rotación alrededor del eje Y.
 * @param angle Ángulo de rotación (en radianes).
 * @return Matriz de rotación 3x3.
 */
Matrix R_y(double angle);

#endif